import { async } from '@angular/core/testing';
import {DateService} from './date.service';

describe('DateService', () => {
  let service;


  beforeEach(() => {
    service = new DateService();
  });


  it('should run #actual_date()', async () => {
    // const result = actual_date();
  });

  it('should run #actual_day()', async () => {
    // const result = actual_day();
  });

  it('should run #actual_hour()', async () => {
    // const result = actual_hour();
  });

});